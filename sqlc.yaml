version: "2"
sql:
  - schema: "migrations"
    queries: "query"
    engine: "postgresql"
    gen:
      go:
        out: "internal/db"
        package: "db"
        sql_package: "pgx/v5"
        emit_json_tags: true
